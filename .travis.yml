branches:
  only:
      - master

language: c

compiler:
  - gcc
  - clang

before_install:
  - sudo apt-get install -y libcurl4-openssl-dev libgpgme11-dev libxml2-dev

script:
  - autoreconf
  - ./configure
  - make
  - make install

notifications:
  email:
    recipients: tbrown@freeshell.org
    on_success: change
    on_failure: always


